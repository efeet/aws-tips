files:
  "/tmp/revision.sh":
    mode: "000700"
    owner: root
    group: root
    content: |
      #!/bin/sh
      >/var/log/os.log
      date >> /var/log/os.log
      crontab -l >> /var/log/os.log

commands:
  01-revision:
    command: "/tmp/revision.sh"